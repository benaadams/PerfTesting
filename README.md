Enter project director in src folder
```
dotnet restore
dotnet run
```

e.g.

```
cd src\BoxingTest
dotnet restore
dotnet run
```

The above example will do two runs; so you'll have to scroll up to see first